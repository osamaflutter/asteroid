import 'package:flutter/material.dart';

class AppColors {
  // Background / Dark Space Theme
  static const Color background = Color(0xFF0D0D0D);
  static const Color surface = Color(0xFF1A1A2E);

  // Text
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFE0E0E0);

  // Danger / Blast / Impact
  static const Color blastInner = Color(0xFFFF6B6B); // red
  static const Color blastOuter = Color(0xFFFFA500); // orange
  static const Color impactMarker = Color(0xFFFF0000); // bright red

  // Safe / Interactive Controls
  static const Color primaryButton = Color(0xFFFF6B6B); // same as blastInner
  static const Color secondaryButton = Color(0xFF4FC3F7); // cyan / info
  static const Color sliderActive = Color(0xFF4FC3F7);
  static const Color sliderInactive = Color(0xFF1A1A2E);

  // Optional Trajectory / Orbital Path
  static const Color trajectory = Color(0xFF81C784); // green
}